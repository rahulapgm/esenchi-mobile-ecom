import React from "react";

const TextField = props => {
  const {
    isGradient,
    gradColors,
    gradStart,
    gradEnd,
    gradStyle,
    viewStyle,
    textStyle,
    onPress,
    onHover,
    btnText,
    disabled,
    theme,
    ...others
  } = props;

  return null;
};

export default TextField;
