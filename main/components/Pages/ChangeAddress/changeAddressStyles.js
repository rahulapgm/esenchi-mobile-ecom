import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  brandIcon: {
    height: 56,
    width: 80
  },
  inputTextStyle: { backgroundColor: "white", paddingTop: 12 }
});

export default styles;
